spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
  flyway:
    enabled: false
otel:
  instrumentation:
    common:
      default-enabled: false
  sdk:
    disabled: true